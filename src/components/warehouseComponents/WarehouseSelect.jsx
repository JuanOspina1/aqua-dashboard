import React from "react";

// Select Options: show full inventory / select warehouse / create a warehouse / delete a warehouse / create an item / delete an item

// Select a warehouse from the db
// Map over db of warehouses to create them

const WarehouseSelect = () => {
  return <div>WarehouseSelect</div>;
};

export default WarehouseSelect;

import React from "react";

// INPUTS: CURRENT WAREHOUSE / RELEASE TO / CONSIGNEE / PO / RELEASE DATE / RELEASE ON PALLETS? / # OF PALLETS / REP / ARRAY OF LOTS WITH CASE COUNTS

const NewOrderForm = () => {
  return <div>NewOrderForm</div>;
};

export default NewOrderForm;
